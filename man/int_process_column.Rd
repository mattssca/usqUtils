% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/int_process_column.R
\name{int_process_column}
\alias{int_process_column}
\title{Process and Standardize a Column in a Data Frame}
\usage{
int_process_column(
  data = metadata,
  column,
  new_name = NULL,
  type = "character",
  factor_levels = NULL,
  factor_order = NULL,
  replace_na = NULL,
  rename_factors = NULL,
  boolean_map = NULL,
  domain = NULL,
  print_table = TRUE,
  log_changes = TRUE
)
}
\arguments{
\item{data}{A data frame to operate on. Defaults to \code{metadata}.}

\item{column}{The name of the column to process (character).}

\item{new_name}{Optional new name for the column (character).}

\item{type}{Desired type for the column. Options: \code{"character"}, \code{"factor"}, \code{"numeric"},
\code{"double"}, \code{"integer"}, \code{"boolean"}, \code{"date"}. Defaults to \code{"character"}.}

\item{factor_levels}{Optional vector of factor levels (character).}

\item{factor_order}{Optional vector specifying the order of factor levels (character).}

\item{replace_na}{Optional vector of values to replace with \code{NA} (character).}

\item{rename_factors}{Optional named vector for renaming factor levels (named character).}

\item{boolean_map}{Optional list specifying values for \code{TRUE} and \code{FALSE} when type is \code{"boolean"}.}

\item{domain}{Optional domain/category for the column (character).}

\item{print_table}{Logical; if \code{TRUE}, prints a table of the column's values after processing.
Default is \code{TRUE}.}

\item{log_changes}{Logical; if \code{TRUE}, logs the changes to the global \code{column_change_log} list.
Default is \code{TRUE}.}
}
\value{
The updated data frame with the processed column.
}
\description{
This helper function processes a specified column in a data frame, allowing for renaming, type
conversion, factor level management, NA replacement, and logging of changes.
}
\details{
\itemize{
\item Renames the column if \code{new_name} is provided.
\item Converts the column to the specified type.
\item For factors, can set levels, rename levels, and set order.
\item For booleans, requires a \code{boolean_map} list.
\item Replaces specified values with \code{NA}.
\item Logs changes to \code{column_change_log} if enabled.
}
}
\examples{
int_process_column(data = df, 
                   column = "old_col", 
                   new_name = "new_col", 
                   type = "factor", 
                   factor_levels = c("A", "B"))

}
